<nav class="navbar navbar-light" style="background-color: #e3f2fd;">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BookMyShow</a>
    <% if customer_signed_in? %>
      <a class="nav-link active text-default ms-20  " aria-current="page" href="#"><i class="fa fa-user-circle" aria-hidden="true"></i> <%= current_customer.email%></a>
      <div class="dropdown me-auto">
        <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
          settings
        </a>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
          <h5 class="d-flex ms-auto order-5"> <%= link_to "logout",destroy_customer_session_path, data: { turbo_method: :delete },class:"dropdown-item"  %>
          </h5>
        </ul>
      </div>
    <% end %>
    
    <%= form_with url: "/movies", method: :get , class:"d-inline-flex ms-auto" do |form| %>
      <%= form.text_field :movie, class: "form-control me-2 d-inline-flex"%>
      <%= form.submit "Search", class:"btn btn-outline-success" %>
    <% end %>
  </div>
</nav>
